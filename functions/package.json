{
    "name": "codemax-functions",
    "version": "1.0.0",
    "main": "lib/index.js",
    "scripts": {
        "build": "tsc",
        "deploy": "npm run build && firebase deploy --only functions"
    },
    "dependencies": {
        "firebase-admin": "^12.1.0",
        "firebase-functions": "^4.9.0",
        "node-fetch": "^2.7.0"
    },
    "devDependencies": {
        "typescript": "^5.4.0"
    },
    "engines": {
        "node": "18"
    }
}